# Rideau Sensor Simulation

This project simulates a Rideau water-level sensor and sends telemetry data to an Azure IoT Hub device. It helps test cloud integration without a physical sensor.

---

## 1. Requirements

- Python 3.10 or higher  
- Install the required package:

```bash
pip install azure-iot-device
